var numId;
var num;

//panel body on click

$("#panel-body").on("click", function(){
    
    numId = ($(this).attr("id"));
    num = parseInt(numId.text());
    
    //if element.text === num 
        // store in num array
    
});